{% extends '::frontend.html.twig'%}

{% block title %} INICIO {% endblock%}
{% block id 'portada' %}
{% block article%}
<section id="menu_admin">
    {% include 'administrativoBundle:Default:menu_admin.html.twig'%}
</section>
        
{#LISTA DE MATERIAS: #}

<strong><p>Listado de materias</p></strong>
<div id="dt_example">
    <div id="demo">
    {% if sd== 1 %}
        <form name="diurna" method="post" {{form_enctype(formmaterias) }}>
            {{ form_start(formmaterias) }}
        <table class="display" id="example">
            <thead>
                <tr >
                    <th>{{ 'Nombre'}}</th>
                    <th>{{ 'Area' }}</th>
                    <th>{{ 'Numero Horas' }}</th>
                    <th>{{ 'Descripcion' }}</th>
                    <th>{{ 'Estado' }}</th>                 
                 </tr>
                </thead>
                            
        {# iterate over each existing tag and render its only field: name #}
        {% for mat in formmaterias.materiaadmin %}
<tbody>
    <tr>            
        <td ALIGN="CENTER" style="vertical-align:middle">{{ form_widget(mat.nombre) }}</td>                
        <td ALIGN="CENTER" style="vertical-align:middle">{{ form_widget(mat.area ) }} </td>
        <td ALIGN="CENTER" style="vertical-align:middle">{{ form_widget(mat.numerocreditos)}}</td>
        <td ALIGN="CENTER" style="vertical-align:middle">{{ form_widget(mat.descripcion) }}</td>
        <td ALIGN="CENTER" style="vertical-align:middle">{{ form_widget(mat.estado) }}</td>        
    </tr>
</tbody>                       
    {% endfor%}
     
                        
        </table>
            <div class="paginacion">
                        {{ knp_pagination_render(materias) }}
                     </div>
    <input type="submit" value="guardar" class="boton" >       
    {{ form_end(formmaterias) }}            
    
    </form>
            {%else%}
        <h1>No hay materias</h1>
    {% endif%}
    </div>
</div>
<br>

{% endblock%}
{% block aside %}
{{parent()}}
    
{% endblock%}

          
{% block javascripts %}
    {{ parent() }}

{% endblock%}


{% block stylesheets %}
{{ parent() }}
         {% include 'academicoBundle:Default:tablecss.html.twig'%}

<style>
   
  input[type="number"] {
   width:50px;
}
</style>



   {% endblock %}
     

    
